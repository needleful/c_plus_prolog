:- include(stdio, stdlib).

int func add(int var (a, b)) => return a+b.

int func main(int var argc, char:ptr:array var argv) =>
	if argc < 3 then (
		printf("Usage: `%s <number> <number>`", argv:0); 
		return 1 );
	int var (
		a = atoi(argv:1),
		b);
	b = atoi(argv:2);
	printf("%d + %d = %d!", a, b, add(a,b));
	return 0.
